name: Keep Backend Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl --fail --silent --show-error --max-time 90 "${{ vars.RENDER_BACKEND_URL }}/"
